# dsh-shutdown-after-task bundle patch: insert the plugin row over the profile root.
# The `name` resolves through the profile's node_modules after `dsh plugin add`.
- insert:
    - id: dsh-shutdown-after-task
      name: 'dsh-shutdown-after-task'
